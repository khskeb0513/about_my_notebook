module.exports = {
    apps: [{
        name: 'about',
        script: 'npm start',
        env: {
            NODE_ENV: 'production'
        }, env_production: {
            NODE_ENV: 'production'
        }
    }]
};
